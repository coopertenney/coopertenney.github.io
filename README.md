# coopertenney.org

Personal site. Single static `index.html`, served by GitHub Pages from `main`.

Edit `index.html`, push to `main`; Pages redeploys in about a minute. Résumé lives at `resume.pdf`.
